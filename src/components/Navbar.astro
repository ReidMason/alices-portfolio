---
import NavigationItems from "./NavigationItems.astro";
import SocialMediaLinks from "./SocialMediaLinks.astro";
---

<nav class="text-xl flex gap-12 items-baseline w-full mb-14 md:mb-28">
  <a class="text-4xl" href="/">Alice Collins</a>
  <!-- Burger menu toggle -->
  <div class="ml-auto md:hidden">
    <svg
      id="burger-menu-toggle"
      xmlns="http://www.w3.org/2000/svg"
      fill="none"
      viewBox="0 0 24 24"
      stroke-width="1.5"
      stroke="currentColor"
      class="w-8 h-8"
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        d="M3.75 9h16.5m-16.5 6.75h16.5"></path>
    </svg>
  </div>
  <!-- Burger menu -->
  <div
    class="hidden top-24 left-0 absolute flex flex-col gap-6 items-center bg-white w-full h-full"
    id="burger-menu"
  >
    <NavigationItems />
  </div>
  <!-- Navigation items -->
  <div class="hidden md:flex gap-4">
    <NavigationItems />
  </div>
  <!-- Social links  -->
  <div class="gap-3 ml-auto hidden md:flex">
    <SocialMediaLinks />
  </div>
</nav>

<script>
  const burgerMenuToggleElement = document.getElementById("burger-menu-toggle");
  const burgerMenu = document.getElementById("burger-menu");
  const body = document.getElementById("body");

  if (burgerMenuToggleElement) {
    burgerMenuToggleElement.addEventListener("click", () => {
      if (!burgerMenu || !body) return;

      burgerMenu.classList.toggle("hidden");

      if (burgerMenu.classList.contains("hidden")) {
        body.classList.remove("overflow-hidden");
      } else {
        body.classList.add("overflow-hidden");
      }
    });
  }
</script>
